#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main() {

    pid_t pid = getpid();
    printf("Current Process ID: %d\n", pid);

    pid_t ppid = getppid();
    printf("Parent Process ID: %d\n", ppid);

    return 0;
}
